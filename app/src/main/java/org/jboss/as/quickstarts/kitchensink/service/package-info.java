/**
 * Provides service layer classes that encapsulate business logic.
 *
 * <p>Services orchestrate operations, interact with repositories, and may publish events. They are
 * typically transactional.
 */
package org.jboss.as.quickstarts.kitchensink.service;
